module.exports = {
    darkMode: 'class', // ✅ this is the key fix
    content: ['./**/*.html', './**/*.js'], // or your actual file paths
    theme: {
      extend: {},
    },
    plugins: [],
  };